{% extends 'base.html.twig' %}

{% block title %}Analyses{% endblock %}

{% block body %}
        {% include "menu.html.twig" %}
<div class="page-wrapper">
    <div class="content">
		<div class="row">
			<div class="col-sm-4 col-3">
				<h4 class="page-title">Analyses</h4>
			</div>
			<div class="col-sm-8 col-9 text-right m-b-20">
				<a href="{{path('resultat_index')}}" class="btn btn btn-primary btn-rounded float-right"><i class="fa fa-plus"></i>Resultats</a>
				<a href="{{path('ordonnance_index')}}" class="btn btn btn-primary btn-rounded float-right"><i class="fa fa-plus"></i> Ordonnances</a>
			{% if is_granted('ROLE_MEDECIN') %}
				<a href="{{path('analyse_create')}}" class="btn btn btn-primary btn-rounded float-right"><i class="fa fa-plus"></i> Ajouter Analyse</a>
			{% endif %}
			</div>
		</div>
		</div>
		<div class="row">
			<div class="col-md-12">
				<div class="table-responsive">
					<table class="table table-border table-striped custom-table mb-0">
						<thead>
							<tr>
								<th>Prescription</th>
								<th>Libelle</th>
								<th>Details</th>
								<th>Date</th>
								<th class="text-right">Action</th>
							</tr>
						</thead>
						<tbody>
							<tr>
								<td>prescription demangeaison</td>
								<td>sell</td>
								<td>ddhugjhik yhujiko rtgyhujik fgcvhbjnk n</td>
								<td>2020-10-14 01:33:14</td>
								<td class="text-right">
									<div class="dropdown dropdown-action">
										<a href="#" class="action-icon dropdown-toggle" data-toggle="dropdown" aria-expanded="false"><i class="fa fa-ellipsis-v"></i></a>
										<div class="dropdown-menu dropdown-menu-right">
											<a class="dropdown-item" href="{{asset('hopital_update')}}"><i class="fa fa-pencil m-r-5"></i> Edit</a>
											<a class="dropdown-item" href="#" data-toggle="modal" data-target="#delete_schedule"><i class="fa fa-trash-o m-r-5"></i> Delete</a>
										</div>
									</div>
								</td>
							</tr>
						</tbody>
					</table>
				</div>
		</div>
	</div>
</div>
{% endblock %}
